"use client";

type Props = {
  description: string;
};

export default function ProductDescription({
  description,
}: Props) {
  return (
    <section>
      {/* En el siguiente paso moveremos aquí el contenido */}
    </section>
  );
}